const fs=require('fs');let h=fs.readFileSync('how-it-works.html','utf8');h=h.replace('>Structured records only</span>','>Firm-controlled recording policy</span>');h=h.replace('>Deterministic intake logic</span>','>Deterministic intake controls</span>');h=h.replace('Benjamin walks through a structured, practice-specific intake path &mdash; asking yes/no questions only.','Benjamin walks through a structured, practice-specific intake path using pre-approved question branches configured for the firm. The flow can support yes/no questions, short structured responses, and open-ended caller responses where the firm has approved them.');fs.writeFileSync('how-it-works.html',h,'utf8');console.log('partial 1 done')